# onestop-site

Staging area for the OneStop **review hub** (GitHub Pages). Only flattened, self-contained review pages live here — never the working Jaarvis vault or its generation scratch.

Publish: `python3 tools/publish.py --title "..." --in <deck>.html --slug <slug> --push`
